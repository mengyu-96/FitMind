# 本地环境与验证记录

版本：v1.0｜日期：2026-09-25｜状态：环境创建与本地冒烟验证已完成

## 1. 授权与执行范围

用户确认C01—C10，明确允许创建适配后端的Conda环境，并要求优化Agent架构。本次完成独立环境、依赖安装、版本锁定、验证脚本与设计同步。未接入真实模型、未安装数据库服务、未发布应用。

## 2. 环境

| 项目 | 实际结果 |
|---|---|
| Conda入口 | D:\anaconda\Scripts\conda.exe，26.7.1 |
| 原base Python | 3.14.7，未安装项目包到base |
| 项目环境 | D:\work\FitMind\.conda\fitmind-backend |
| 项目Python | 3.12.14 |
| FastAPI / Uvicorn | 0.141.1 / 0.53.0 |
| Pydantic / Settings | 2.13.5 / 2.15.0 |
| SQLAlchemy / Alembic | 2.1.0 / 1.20.0 |
| Psycopg / pgvector客户端 | 3.3.6 / 0.5.0 |
| Redis客户端 | 7.4.1 |
| LangGraph | 1.2.12 |
| PostgreSQL checkpointer | langgraph-checkpoint-postgres 3.1.2 |
| pytest / pytest-asyncio | 9.1.1 / 1.4.0 |
| Ruff | 0.16.9 |

Conda经conda-forge创建运行时，pip安装应用依赖。初次沙箱执行受网络及Conda缓存权限限制，后经工具权限流程完成安装。环境与包缓存均位于项目隐藏目录并已忽略提交。

## 3. 验证结果

| 检查 | 结果 | 证明范围 |
|---|---|---|
| pip check | 通过 | 已安装依赖声明无冲突 |
| Python路径与版本 | 通过 | 使用项目独立3.12解释器 |
| 数据库/向量/Redis模块导入 | 通过 | 二进制依赖与Python版本可加载 |
| FastAPI本地TestClient | 通过 | 有效输入200，无效训练次数422 |
| LangGraph interrupt | 通过 | 未确认时不产生approved结果 |
| LangGraph resume接受/拒绝 | 通过 | 重建图对象后可使用同一内存检查点继续 |
| 两个thread状态分离 | 通过 | 接受A不改写待确认B；仅框架状态测试 |
| Ruff静态检查与格式 | 通过 | 环境验证脚本无当前规则违规 |

验证脚本在[backend/scripts/verify_environment.py](../backend/scripts/verify_environment.py)，执行说明见[后端环境README](../backend/README.md)。检查脚本显式关闭远程追踪，不需要模型API密钥。

## 4. 可重建资产

- [运行时规格](../backend/environment.yml)：Python3.12与pip。
- [Windows运行时精确导出](../backend/conda-win-64.explicit.txt)：本次Conda运行时包。
- [直接依赖范围](../backend/requirements.in)：应用及开发工具。
- [pip精确版本清单](../backend/requirements.lock.txt)：已安装版本快照，非hash锁定。

Conda安装的packaging元数据含构建机本地路径，生成pip版本清单时已规范化为实际安装版本`packaging==26.3`，避免另一台机器尝试读取不存在的构建目录。

## 5. 尚未验证的边界

当前PATH未发现git/node/npm/docker/psql/redis-server命令，也未发现名称匹配PostgreSQL、Redis、Docker的Windows服务；这不等同于扫描所有磁盘证明软件未安装。数据库、Redis服务和前端工具在对应实施阶段继续检查或配置。

本次不证明PostgreSQL/Redis可连接、checkpointer跨进程持久化、用户鉴权、工具业务幂等、租约并发或真实模型效果。以上仍按11和19的集成验收执行。当前业务功能未实现，不能据此宣称MVP或G1已完成。
